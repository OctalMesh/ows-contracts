type: "object"
required:
  - "items"
  - "subtotal"

properties:
  items:
    type: "array"
    items:
      $ref: "./CartItem.yaml"

  subtotal:
    $ref: "../../../shared/components/schemas/Money.yaml"

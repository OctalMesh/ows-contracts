type: "object"
required:
  - "items"
  - "pagination"

properties:
  items:
    type: "array"
    items:
      $ref: "./Address.yaml"

  pagination:
    $ref: "../../../shared/components/schemas/Pagination.yaml"

openapi: "3.1.2"

info:
  version: "0.1.0"
  title: "Search Service API"
  termsOfService: "https://octalmesh.com/articles/terms"
  contact:
    name: "OctalMesh Support"
    url: "https://octalmesh.com"
    email: "contact@octalmesh.com"
  license:
    name: "MIT"
    url: "https://opensource.org/license/MIT"

servers:
  - url: "/api"

tags:
  - name: "Search"
    description: "Full-text product search, filtering, and sorting."

paths:
  /search/products:
    $ref: "./paths/search-products.yaml"

components:
  schemas:
    SearchResult:
      $ref: "./components/schemas/SearchResult.yaml"

  parameters:
    Query:
      $ref: "./components/parameters/QueryParam.yaml"
    Limit:
      $ref: "../shared/components/parameters/LimitParam.yaml"
    Page:
      $ref: "../shared/components/parameters/PageParam.yaml"

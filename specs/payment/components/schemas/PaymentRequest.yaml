type: object
required: [orderId, amount, paymentMethodToken]
properties:
  orderId:
    type: string
    format: uuid
  amount:
    $ref: ../../../shared/components/schemas/Money.yaml
  paymentMethodToken:
    type: string

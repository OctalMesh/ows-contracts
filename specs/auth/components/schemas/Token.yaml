type: "object"
required:
  - "accessToken"
  - "tokenType"
  - "expiresIn"

properties:
  accessToken:
    type: "string"

  tokenType:
    type: "string"
    enum:
      - "Bearer"

  expiresIn:
    type: "integer"
    format: "int32"
    minimum: 1
